{
  "name": "jira-ticket-cluster-alert",
  "version": "1.0.0",
  "description": "Standalone Jira ticket monitor that alerts on 3+ similar IT-* summaries",
  "main": "dist/src/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "http://localhost:26831/RainerK64/jira-ticket-cluster-alert"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2",
    "electron": "^44.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2"
  }
}
